#include <bits/stdc++.h>
#include <iostream>
using namespace std;
bool chechCount(int a, vector<int> &nums)
{

} 
int specialArray(vector<int> &nums)
{
    sort(nums.begin(), nums.end());
    int d = nums.size();
    int l = nums[d - 1];
    for (int i = 0; i<d; i--)
    {
        
    }
}
int main()
{

    return 0;
}